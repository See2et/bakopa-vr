language: ja
default_piece: cc-sdd-impl-codex
log_level: info
provider: codex
model: codex
branch_name_strategy: romaji
pipeline:
  default_branch_prefix: "takt/"
  # NOTE: commit message is generated in the piece; this is a fallback only.
  commit_message_template: "chore: {title}"
  pr_body_template: |
    ## Summary
    {report}
